spring:
  application:
    name: multas-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/biblioteca_multas?createDatabaseIfNotExist=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    web-application-type: servlet
server:
  port: ${SERVER_PORT:8085}
prestamos:
  service:
    url: ${PRESTAMOS_SERVICE_URL:http://localhost:8082}
